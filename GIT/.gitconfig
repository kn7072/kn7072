[user]
	name = stepan
	email = stepan@yandex.ru
[pull]
	ff = true
[core]
	autocrlf = true
[merge]
    tool = extMerge
[mergetool "extMerge"]
    cmd = extMerge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
    trustExitCode = false
[diff]
    external = extDiff
